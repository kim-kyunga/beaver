Drop one WSI here, e.g. `slide.tiff` (`.tiff`/`.svs`/`.mha`).
The handler picks the first file in this folder, so the filename need not match
`inputs.json`. Only the `whole-slide-image` socket slug selects interface 1.
