# The base image (see base.Dockerfile) already provides:
#   trident, torch, torchvision, timm==0.9.16, einops, openslide,
#   segmentation-models-pytorch, transformers, huggingface_hub,
#   pandas, numpy, tifffile, matplotlib, tqdm, Pillow, h5py, opencv-python
# Additional dependencies only:
hydra-core
omegaconf
scikit-learn==1.7.2
joblib
seaborn
SimpleITK
tiffslide
